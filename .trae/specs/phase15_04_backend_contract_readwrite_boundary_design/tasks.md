# Tasks

- [x] Task 1: 完成本 spec 三件套并冻结设计正文
  - [x] SubTask 1.1: 撰写 spec.md：`psco.progress.v1` 合同设计（注释版 proto 草案：3 枚举 + `ProgressEvent` 11 字段 + 3 RPC envelope + `ProgressService` 无 Update 显式声明 + 7 项合同设计决策冻结）；3 RPC 错误语义三要素（含 `REPOSITORY_NOT_FOUND` / `INVALID_PROGRESS_EVENT_ID` 新码 + Create/List 错误语义辨析留档 + DP-2 裁决：`candidate/RepositoryReader.RepositoryExists` 纯事实查询 + service 层语义包装 + FK RESTRICT 兜底声明）；Go 模块分层（根包 4 文件含 types.go 草案 + 四子包 + connecterrors 登记点 + platform 装配点）；`ProgressReader` 接口签名（消费方拥有 + 实现位 + 装配接线 + 4 项设计决策）；brief `progress = 9` 装配演进点（BriefProgress 字段号 1-4 + import + Go 编排步骤 6 + 转换函数复用）；与 phase15-02/03 单值一致与不偷渡声明
    - 执行记录：spec.md 已按 6 项 ADDED Requirements 完成——①proto 草案基于实读 standard.proto（文件头注释结构 / 枚举 UNSPECIFIED=0 / rpc 失败语义注释 / go_package / 生成产物注释）与 project_context.proto（import 模式 / reserved 声明）转写；②DP-2 裁决基于实读 standard/candidate/target_reader.go（EnsureTargetExists 的"target 不存在归 InvalidArgument"模式），改进为 RepositoryExists 纯 bool 事实查询 + service 层语义包装（List/Create 两路径不同错误语义的承接需求）；③哨兵错误清单与 connecterrors 登记基于实读 connect_errors.go（按哨兵 errors.Is 匹配 + Internal 兜底 `_ =` 引用模式）；④Go 分层基于实读 standard 模块（types.go 受控枚举与读/写模型 / errors.go / validate.go / service/connect/repository/candidate 四子包）；⑤ProgressReader 基于实读 projectcontext/candidate/context_readers.go（StandardReader 消费方拥有 + platform 注入 + domain 类型透传）与 projectcontext/service/query_service.go（GetProjectBrief 编排步骤与失败语义注释模式）；⑥brief 装配基于实读 projectcontext/types.go（ProjectBriefReadResult.Standards domain 透传 + "proto 组装在 Connect handler 内复用 standard/connect 导出的转换函数"注释）与 projectcontext/connect/server.go（DomainStandardToProto 复用实例 L87）；⑦错误语义辨析基于实读 project_context.proto GetProjectBrief 错误语义（仓库不存在 → CodeNotFound）与 shared_baseline §3.2（不存在 → 创建时 invalid_argument）两个冻结上游
  - [x] SubTask 1.2: 撰写 tasks.md / checklist.md（本文件与 checklist）
    - 执行记录：tasks.md 含 3 任务 / 7 子任务；checklist.md 含 15 检查点（独立复核发现的计数笔误已修正：原误记 14）；依赖关系与 dev_plan §5 一致（phase15-06 depends on 本 spec + phase15-03；phase15-05 消费本 spec RPC envelope 与错误码）

- [x] Task 2: 一致性校验
  - [x] SubTask 2.1: 与语义上游单值一致校验：proto 草案枚举值域 vs phase15-03 DDL CHECK 三列；`ProgressEvent` 11 字段 vs DDL 11 列；Create 校验执行序与错误码 vs phase15-03 冻结总表（零改写 + 新码仅承接显式留位）；`ProgressSummary` 四字段 vs phase15-02 BriefProgress 字段清单；`REPOSITORY_NOT_FOUND` InvalidArgument 语义 vs shared_baseline §3.2"不存在 → 创建时 invalid_argument"；List NotFound 语义 vs GetProjectBrief 读锚点惯例
    - 执行记录：逐项比对通过——①3 枚举值域与 phase15-03 DDL CHECK 三列单值（workflow_type: phase/audit/fix ↔ WORKFLOW_TYPE_*；event_kind: 四值 ↔ EVENT_KIND_*；source: manual/git/agent ↔ PROGRESS_SOURCE_*）；②ProgressEvent 11 字段号 1-11 与 DDL 11 列按序一一对应（id→created_at），可空三字段（task_key/detail/evidence_ref）空串语义与 DDL NULL 等价声明一致；③Create 校验执行序 6 步与错误码完全引用 phase15-03（9 业务码 + 2 envelope 码零改写）；REPOSITORY_NOT_FOUND 仅承接 phase15-03 执行序第 6 步显式留位（"承接位归 phase15-04 DP-2 裁决"原文），非改写；INVALID_PROGRESS_EVENT_ID 为 Delete 路径新码（Delete 无 phase15-03 输入结构，不在其执行序范围，显式说明防 1:1 破坏）；④ProgressSummary 四字段与 phase15-02 BriefProgress 字段清单 1:1（current_phase_key/current_phase_label/latest_task_completed/recent_events），latest_task_completed 指针可空 ↔ proto"零值不设置"，RecentEvents 恒非 nil ↔ 空态恒构造；⑤REPOSITORY_NOT_FOUND → InvalidArgument 与 shared_baseline §3.2"不存在 → 创建时 invalid_argument"冻结一致；⑥List 不存在 → NotFound 与 project_context.proto GetProjectBrief 错误语义注释（"仓库不存在：返回 CodeNotFound"）一致；Create/List 辨析显式留档（两语义各有冻结上游，不做统一化改写）；⑦BriefProgress 字段号 1-4 自然序为本 spec 冻结项（phase15-02 显式保留："字段号分配与消息定义落点细节归 phase15-04"）；progress = 9 与槽位 2/3/4 保持 reserved 均为 phase15-02 冻结值
  - [x] SubTask 2.2: 与既有实现模式一致校验：proto 草案注释结构 vs standard.proto（文件头 / 枚举 UNSPECIFIED=0 / optional 模式 / rpc 失败语义注释）；Go 分层 vs standard 模块（根包 types/errors/validate + connect/service/repository/candidate）；ProgressReader vs StandardReader（消费方 candidate 拥有 + service 实现 + platform 注入 + domain 类型透传）；DomainProgressEventToProto 导出 vs DomainStandardToProto；connecterrors 登记模式；buildProgress / mountProgressConnect vs buildStandard / mountStandardConnect；git 工作区确认零代码 / 零 proto / 零迁移文件 / 零根级改动
    - 执行记录：逐项比对通过——①proto 草案文件头（文档定位 / 上游规格 / 合同约束 / 生成入口四段注释）、枚举 UNSPECIFIED=0 + SCREAMING_SNAKE_CASE、rpc 失败语义注释格式、option go_package 分号别名（progressv1）均沿 standard.proto 实读模式；source optional 沿 CreateStandardRequest.status optional 模式；②根包 types/errors/validate + 四子包与 standard 模块逐文件对照（validate.go/derive.go 落根包沿 standard validate.go 模式；types.go 不 import 生成 pb 包沿 standard types.go 约束注释）；③ProgressReader 接口定义落 projectcontext/candidate/context_readers.go 与 StandardReader 同文件、注释结构（"消费方拥有的 candidate 接口 + platform 装配点注入 + 不得直接书写 SQL"）逐句对齐 StandardReader 注释模式；实现位 progress/service.QueryService 对应 standard/service.QueryService.ListStandardsByRepository 实现位模式；ProjectBriefReadResult.Progress 值类型 domain 透传沿 Standards []standard.StandardReadResult 模式；④DomainProgressEventToProto 导出命名沿 standard/connect/server.go DomainStandardToProto（L235 实读）模式，projectcontext/connect 复用方式沿 server.go L87 实读实例；⑤connecterrors 登记沿实读 connect_errors.go 三组追加模式（NotFound isAny 列表 / InvalidArgument isAny 列表 / Internal 兜底 `_ =` 显式引用）；⑥buildProgress/mountProgressConnect 与实读 router.go buildStandard（L425）/mountStandardConnect（L310）逐句对应；buildProjectContext 签名演进沿 standardReader 注入模式（router.go L413 实读）；⑦git status --porcelain 复验——工作区仅 `.trae/specs/phase15_04_backend_contract_readwrite_boundary_design/` 目录 untracked 新增，零 backend / frontend / database / proto 改动，零根级回写

- [x] Task 3: 独立复核与收口
  - [x] SubTask 3.1: 子代理独立复核（proto 草案可逐字转写性 / 3 RPC 错误语义完备性〔每错误分支可断言、Create vs List 辨析成立〕/ DP-2 裁决自洽性 / 分层与装配模式一致性 / ProgressReader 与 BriefProgress 单值性 / 与 phase15-02/03 零漂移 / 无 phase15-05/06 偷渡 / tasks-checklist 质量与勾选真实性）
    - 执行记录：已指定独立复核子代理对三件套执行八维度复核（实读 5 份上游文档 + 9 处既有实现参照 + git 工作区实测）；复核报告结论为 **PASS（0 阻断性问题）**；3 项非阻断性观察留档——①checklist 检查点计数笔误（原记 14 实为 15，已修正）；②derive.go 为 progress 特有落点（standard 无逐文件对应物，spec 正文"沿 standard 模块模式"口径为准，phase15-06 实现按此理解）；③DeriveProgressSummary 参数类型名 phase15-03 伪码占位名（ProgressEventRecord）与 phase15-04 冻结名（ProgressEventReadResult）不同——Go 类型名权威落点在本 spec（phase15-03 显式不承载 Go 类型签名），phase15-06 实现以本 spec 命名为准
  - [x] SubTask 3.2: 修复独立复核发现的阻断性问题（如有）并复验
    - 执行记录：无阻断性问题需修复（复核 0 blocker）；非阻断观察 ①已顺手修正（计数笔误），②③为实现期理解口径说明（已留档于 SubTask 3.1 执行记录），无需改 spec 正文；无需复验轮次
  - [x] SubTask 3.3: tasks.md / checklist.md 全部勾选附执行记录；变更保持未提交，待用户最终确认后手动提交
    - 执行记录：tasks.md 3 任务 / 7 子任务全部勾选并附执行记录；checklist.md 15 检查点全部勾选；git status --porcelain 复验——工作区仅本 spec 目录 `.trae/specs/phase15_04_backend_contract_readwrite_boundary_design/` untracked，零代码 / 零 proto / 零迁移文件 / 零根级改动；全部变更保持未提交，待用户最终确认后手动提交

# Task Dependencies

- Task 1（三件套产出）为本 spec 获用户批准后的首个执行项
- Task 2 depends on Task 1
- Task 3 depends on Task 2
- 后续：phase15-06 depends on 本 spec + phase15-03（dev_plan §5）；phase15-05 前端设计消费本 spec RPC envelope 与错误码

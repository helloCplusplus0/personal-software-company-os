# Checklist

- [x] 当前项目已被明确冻结为 `Durable System Track`
- [x] 当前正式运行主线已明确为 `React Web + Go Backend + PostgreSQL`
- [x] 前端统一基线已明确为 `React + Vite + TypeScript + TanStack Router + TanStack Query + Zustand + Tailwind CSS + shadcn/ui`
- [x] 后端主线已明确为 `Go + PostgreSQL + 模块化单体 + 单进程单主运行面`
- [x] 部署与运行标准已明确为 `Single Server First + Caddy + systemd`
- [x] TS / Go / Rust 跨语言长期合同标准已明确为 `Protocol Buffers`
- [x] `Rust` 已被明确限定为未来计算扩展位，不进入 `v0.1` 首轮实现
- [x] 当前项目不得重新解释为 `Product Track`
- [x] 当前项目不得继续沿用 `AGENTS-OLD.md` 作为技术栈来源
- [x] 当前项目不得把 `Drizzle` 写成当前 Go 主链的既定实现依赖
- [x] 当前阶段不默认引入微服务、Kubernetes、Docker 全流程、Kafka、Redis 缓存层与 Elasticsearch
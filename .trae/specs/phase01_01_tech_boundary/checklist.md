- [ ] 当前项目已被明确冻结为 `Durable System Track`
- [ ] 当前正式运行主线已明确为 `React Web + Go Backend + PostgreSQL`
- [ ] 前端统一基线已明确为 `React + Vite + TypeScript + TanStack Router + TanStack Query + Zustand + Tailwind CSS + shadcn/ui`
- [ ] 后端主线已明确为 `Go + PostgreSQL + 模块化单体 + 单进程单主运行面`
- [ ] 部署与运行标准已明确为 `Single Server First + Caddy + systemd`
- [ ] TS / Go / Rust 跨语言长期合同标准已明确为 `Protocol Buffers`
- [ ] `Rust` 已被明确限定为未来计算扩展位，不进入 `v0.1` 首轮实现
- [ ] 当前项目不得重新解释为 `Product Track`
- [ ] 当前项目不得继续沿用 `AGENTS-OLD.md` 作为技术栈来源
- [ ] 当前阶段不默认引入微服务、Kubernetes、Docker 全流程、Kafka、Redis 缓存层与 Elasticsearch

# Checklist

- [x] `CreateModule` 的最小表单字段已明确冻结为 `name / description / status`
- [x] 当前阶段已明确创建时不要求同步完成 `Product` 绑定、`Repository` 映射或 `Release` 登记
- [x] 列表空状态已明确承接首个模块登记入口
- [x] 空状态文案目标已明确对准“完成首个模块登记”
- [x] 非空列表状态下仍保留 `CreateModule` 入口
- [x] `Module Registry / List -> Module Create -> Module Detail` 的最小闭环已明确
- [x] 创建成功后的默认后续动作已明确
- [x] 取消创建或创建失败时的返回路径已明确
- [x] 当前阶段未把复杂导入写入创建主线
- [x] 当前阶段未把自动扫描代码写入创建前提
- [x] 当前阶段未把 AI 建议写入首轮必需入口
- [x] 第一版前端正式承接位已明确冻结为 `Repository detail`
- [x] `repository_id` 已明确冻结为第一版唯一正式前端读取锚点
- [x] 第一版已明确禁止独立“项目治理画像”一级页面或第二入口
- [x] `Repository detail` 内的治理画像信息架构已明确冻结为“概览层 / 结构化维护层 / 摘要回看层”
- [x] 第一版已明确避免重演 `phase12` 的大块常驻解释型 UI
- [x] `phase12` 遗留“项目上下文 / 共享项目上下文”设计已明确冻结为系统性退出前端，而不是并区或换名保留
- [x] `Repository detail` 现有“项目上下文”区必须移除
- [x] `Decision detail` 现有“共享项目上下文入口”卡片必须移除
- [x] 第一版允许手工维护的字段集合已明确冻结，且与 `phase13-05` 写边界一致
- [x] 第一版只读展示的字段集合已明确冻结，且不会误入可编辑表单
- [x] `backend / database / frontend / proto` 顶层目录矩阵的第一版前端来源已正式冻结
- [x] 人类维护、agent 消费与摘要回看的边界已明确冻结
- [x] 摘要回看层不会继续承接 `phase12 project-context` 旧叙事本身
- [x] 目录真实路径不再被误当作面向普通用户的主内容
- [x] `entry_ref` 已明确只能作为轻量定位元数据，而不是前端主阅读内容
- [x] 后续执行者不再需要临场决定“第一版到底挂在 Repository detail 还是另起页面”

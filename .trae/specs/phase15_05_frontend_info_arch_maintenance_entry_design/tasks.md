# Tasks

- [x] Task 1: 完成本 spec 三件套并冻结设计正文
  - [x] SubTask 1.1: 撰写 spec.md：DP-1 裁决（当前卡通道 = GetProjectBrief.progress 投影 + 空值统一文案"暂无进行中 phase"不区分两情形 + 完结态承接口径留档 + recent_events web 不消费 + Obs-1 闭环）；DP-3 裁决（datetime-local 控件 + 默认 now + 本地解析转 UTC Timestamp + 本地时区展示 + 不校验未来 + 附件口径归 phase15-08）；切片结构（11 文件清单：data 3 / application 2 / components 4 / types.ts / index.ts + query key + 失效矩阵 + barrel 受控导出 + 切片纪律）；进度区组件树与挂载分区（ProgressSection 挂载 detail 页 StandardReadonlySummary 之后、页面级第三全宽区块、无路由声明）；录入表单交互规格（字段默认值 + 联动禁用矩阵 + task_key placeholder 矩阵 + event_kind localStorage 记忆 + 校验双层模型 + 提交回流与重置语义 + source 不暴露）；时间轴与删除规格（三轨过滤 + 事件行结构 + 空态文案 + window.confirm 删除确认文案逐字冻结）；移动端适配基线与上游单值一致声明
    - 执行记录：spec.md 已按 7 项 ADDED Requirements 完成——①DP-1 裁决基于实读 phase15-01 DP-1 登记行（两候选通道 + "后端统一派生"冻结要求）、phase15-02 派生空值同型语义（"前端不得自行从事件集合计算派生值用于当前卡"封死约束 + Obs-1 移交事项于 SubTask 3.2 执行记录）、phase15-04 ProgressSummary/BriefProgress 输出形态（四字段）与 use-repository-standards-read brief 投影 owner 先例；②DP-3 裁决基于实读 phase15-01 DP-3 登记行（"补录历史可精确复现，rerun 不因时区漂移失败"冻结要求 + 附件口径归 phase15-08 的归属切分）与 phase15-02"occurred_at 未来时间不校验"显式不做约束；③切片结构基于实读 standard 切片 22 文件（find 实测清单）与 shared_baseline §3.6 前端承接矩阵（application / components / data / types.ts / index.ts 五落点单值）；④挂载分区基于实读 repository-binding-detail-page.tsx 全文（L27 StandardReadonlySummary import / L306 挂载 / 页面 space-y-4 容器与 grid lg:grid-cols-3 三列区现状）；⑤表单交互基于实读 use-create-standard.ts（normalizeError 模式 + onSuccess 回调位）与 phase15-03 错误码总表 / phase15-04 合同决策 3（source optional 未设置归一 manual）；⑥删除确认基于实读 standard-detail-page.tsx L45 window.confirm 先例；⑦移动端与紧凑化基于 project memory 既有基线（text-xs / border-t pt-2 / divide-y / 容器仅 :focus-visible / Badge h-4 min-w-4 text-[10px]）
  - [x] SubTask 1.2: 撰写 tasks.md / checklist.md（本文件与 checklist）
    - 执行记录：tasks.md 含 3 任务 / 7 子任务；checklist.md 含 20 检查点（独立复核发现的计数笔误已修正：原误记 19）；依赖关系与 dev_plan §5 一致（phase15-07 depends on 本 spec + phase15-06）

- [x] Task 2: 一致性校验
  - [x] SubTask 2.1: 与语义 / 合同上游单值一致校验：联动禁用矩阵 vs phase15-02 合法矩阵 12 格；task_key placeholder 矩阵 vs K-1~K-4 正则；错误回显 vs phase15-03 错误码总表 + phase15-04 3 RPC 错误语义；DP-1 裁决 vs phase15-01 DP-1 登记冻结要求 + phase15-02"后端统一派生"封死约束 + Obs-1 移交事项；DP-3 裁决 vs phase15-01 DP-3 登记冻结要求 + phase15-02"未来时间不校验"；切片结构 vs shared_baseline §3.6 前端承接矩阵；无路由声明 vs 裁决⑥
    - 执行记录：逐项比对通过（全部比对基于本对话实读的上游文档原文）——①联动禁用矩阵与 phase15-02 合法矩阵 12 格逐格一致（phase 轨 4 格全合法 ↔ 四选项全可选；audit/fix 轨 phase_started/phase_completed 禁止 ↔ 两选项 disabled；audit/fix × task_completed/note 合法 ↔ 可选；§3.6"联动过滤"的 UI 实现形态单值化为 disabled option + 自动重置，语义未收窄未放宽）；②placeholder 矩阵与 K-1~K-4 逐条对应（phaseNN↔K-1 / phaseNN-MM↔K-2 / audit_NNN↔K-3 / fix_NNN↔K-4 / note 自由标注可留空↔K-5）；③错误回显与 phase15-03 错误码总表（9 业务码 + 2 envelope 码）+ phase15-04 3 RPC 错误语义一致（Create 校验失败 InvalidArgument + 写入失败 Internal；Delete INVALID_PROGRESS_EVENT_ID→InvalidArgument / 不存在→NotFound / Internal；List 空→非错误），前端按归一化 message 回显不自行翻译错误码，且不做 repository 存在性预校验（交后端权威，DP-2 承接）；source 不设置→归一 manual 与 phase15-04 合同决策 3 单值；④DP-1 裁决满足 phase15-01 登记冻结要求（通道 = GetProjectBrief.progress 即"后端 service 层派生、web 与 brief 共用同一实现"§3.4 对齐；前端零派生路径），Obs-1 闭环成立（统一文案 = 接受空值同型约束的唯一合规解：区分两情形需前端第二套派生或后端第二套状态字段，均被 phase15-02 冻结禁止）；⑤DP-3 裁决满足 phase15-01 登记冻结要求（录入展示同为浏览器本地时区闭环→rerun 无时区漂移；附件口径显式归 phase15-08 未越界），"不校验未来时间"与 phase15-02 显式不做约束单值；⑥切片结构与 shared_baseline §3.6 前端承接矩阵逐项一致（五落点 application/components/data/types.ts/index.ts 沿 standard 切片结构；最小摩擦三要素 occurred_at 默认 now + event_kind 记住上次 + 非法组合联动全承接；mutation 收敛切片固定承接位）；⑦无路由声明与裁决⑥逐字一致（无独立路由 / 导航项 / Dashboard 主卡片）
  - [x] SubTask 2.2: 与既有实现模式一致校验：切片结构 vs standard 切片 22 文件模式（data/application/components/types/index + query key 命名 + barrel 受控导出）；brief 投影 owner vs use-repository-standards-read 模式；mutation owner 与 normalizeError vs use-create-standard 模式；删除确认 vs standard-detail-page window.confirm 模式（L45）；挂载点 vs repository-binding-detail-page 现状（L27 import / L306 StandardReadonlySummary 之后）；内嵌区组件 vs standard-readonly-summary 模式（区块标题 / 空态 / 错误态 / 加载态四态）；紧凑化规范 vs project memory 既有基线（text-xs / border-t pt-2 / divide-y / 容器仅 :focus-visible）；git 工作区确认零代码 / 零 proto / 零迁移文件 / 零根级改动
    - 执行记录：逐项比对通过——①切片结构与 standard 切片 22 文件实读清单对照（data/application/components/types.ts/index.ts 落点一致；progress 无 pages/ 目录为 shared_baseline §3.6"承接形态为组件级"的设计内差异，非模式偏离；query key 命名沿 ['repository-xxx', repositoryId] 与 ['xxx-list/detail'] 双形态惯例）；②use-repository-progress-read 与 use-repository-standards-read（实读全文）逐句同构（切片自有 owner + projectContextClient.getProjectBrief + 单块投影 + null 过滤）；③use-create-progress-event 与 use-create-standard（实读全文）逐句同构（mutationFn 组装 pb 请求 + normalizeError 归一化 + invalidateQueries 失效 + onSuccess 回调位暴露给组件承接回流）；④删除确认与 standard-detail-page.tsx L45 实读先例一致（window.confirm + 确认文案内嵌 + owner 调用）；⑤挂载点与 repository-binding-detail-page.tsx 实读现状一致（import 区 L27 模式 + StandardReadonlySummary L306 之后追加 + 页面 space-y-4 容器承接区间距）；⑥内嵌区四态（区块标题 text-xs font-medium text-muted-foreground / 加载 / 错误 / 空态文案）与 standard-readonly-summary.tsx 实读模式逐句对齐；⑦紧凑化规范与 project memory 既有基线一致（text-xs / border-t pt-2 / divide-y / min-w-0 truncate / 容器仅 :focus-visible / Badge text-[10px]）；⑧git status --porcelain 实测——工作区仅 `.trae/specs/phase15_05_frontend_info_arch_maintenance_entry_design/` untracked 新增（HEAD=1fbe487 phase15-04 提交），零 frontend / backend / database / proto 改动，零根级回写

- [x] Task 3: 独立复核与收口
  - [x] SubTask 3.1: 子代理独立复核（DP-1 裁决自洽性与 Obs-1 闭环完整性 / DP-3 时区闭环无漂移 / 联动矩阵与合法矩阵 12 格零漂移 / 切片结构与 project_rules §2.5 合规 / 组件树与挂载分区可直接进入实现 / 交互规格完备性〔禁用态 / 重置语义 / 删除确认文案 / 错误回显〕/ 与 phase15-02/03/04 零漂移 / 无 phase15-06/07/08 偷渡 / tasks-checklist 质量与勾选真实性）
    - 执行记录：已指定独立复核子代理对三件套执行八维度复核（实读 phase15-01/02/03/04 spec + shared_baseline + dev_plan + phase14-05 先例 + standard 切片 4 文件 + repository-binding-detail-page 全文 + git 工作区实测）；复核报告结论为 **PASS（0 阻断性问题）**，4 项非阻断性观察留档——①checklist 检查点计数笔误（原记 19 实为 20，已修正）；②"页面级第三全宽区块"序数基数未显式定义（挂载点与分区关系本身单值无歧义，不改文）；③提交按钮禁用条件未显式冻结（沿"前端轻量不阻断 + 后端权威回显"模式可唯一推导为不禁用，phase15-07 按推导口径实现）；④录入控件分钟粒度与 dogfooding 附件秒级精度（git log 真实提交时间含秒）的截断问题——附件口径已正确归 phase15-08，作为 phase15-08 spec 附件明细设计的输入提示留档：附件须注明秒级截断/取整规则以保证 rerun 可复现
  - [x] SubTask 3.2: 修复独立复核发现的阻断性问题（如有）并复验
    - 执行记录：无阻断性问题需修复（复核 0 blocker）；非阻断观察 ①已顺手修正（计数笔误 19→20），②③为实现期理解口径说明（已留档于 SubTask 3.1 执行记录，phase15-07 按推导口径实现），④为 phase15-08 输入提示（已留档，本 spec 无需改动）；无需复验轮次
  - [x] SubTask 3.3: tasks.md / checklist.md 全部勾选附执行记录；变更保持未提交，待用户最终确认后手动提交
    - 执行记录：tasks.md 3 任务 / 7 子任务全部勾选并附执行记录；checklist.md 20 检查点全部勾选；git status --porcelain 复验——工作区仅本 spec 目录 `.trae/specs/phase15_05_frontend_info_arch_maintenance_entry_design/` untracked（HEAD=1fbe487 phase15-04 提交），零代码 / 零 proto / 零迁移文件 / 零根级改动；全部变更保持未提交，待用户最终确认后手动提交

# Task Dependencies

- Task 1（三件套产出）为本 spec 获用户批准后的首个执行项
- Task 2 depends on Task 1
- Task 3 depends on Task 2
- 后续：phase15-07 depends on 本 spec + phase15-06（dev_plan §5：切片实现依赖本设计，浏览器完整会话验证依赖后端运行时前提）

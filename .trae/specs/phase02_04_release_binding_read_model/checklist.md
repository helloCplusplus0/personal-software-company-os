# Checklist

- [x] `CreateRelease` 的最小交互路径已明确冻结
- [x] `CreateRelease` 已明确从 `Module Detail` 进入并回流到当前模块详情上下文
- [x] `BindModuleToProduct` 已明确由 `Module Detail` 直接承接
- [x] `MapModuleToRepository` 已明确由 `Module Detail` 直接承接
- [x] 当前阶段未将绑定动作退化为纯跳转式承接
- [x] `Decision` 在当前阶段已明确只作为只读展示或跳转入口
- [x] 列表页最小读模型已明确
- [x] 详情页最小读模型已明确
- [x] 当前阶段未把 `Decision Center` / `Product Registry` / `Repository Binding` 的全量主线提前并入
- [x] 本次规格与 `phase01-06` 正式 MVP 规格正文保持一致
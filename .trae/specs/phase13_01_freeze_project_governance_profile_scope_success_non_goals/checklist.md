- [x] `phase13` 的单一主交付能力已明确冻结为“项目级治理画像 + 全局规范资产 + agent 项目简报输入”
- [x] `PSCO-native facts / IDE-accessible context / Controlled synced projection` 三层边界已冻结为可直接执行的正式口径
- [x] `phase13` 的成功标准已明确到“执行者无需再猜做到什么算完成”
- [x] `Web / agent` 仍共享同一套 `PSCO-native facts` 已被写成显式成功标准，而不是只靠隐含推导成立
- [x] Git 推进跟踪、模板仓库接入、自动同步、MCP / CLI / agent 写回、目录全文扫描入库与第五个业务主实体已被明确冻结为非目标或禁止事项
- [x] “第二套与四实体并列的事实源”与“继续放大 `phase12` 共享只读 UI 表达”已被显式冻结为非目标或禁止事项
- [x] 后续进入条件已明确写成“仅在 `phase13` 正式收口后再讨论或进入”
- [x] 本 spec 包与 `phase13` 三件套在主交付能力、范围边界、非目标与进入条件上保持单值一致
- [x] 后续执行 `phase13-02 ~ phase13-12` 时，不再需要重新解释 `phase13-01` 的冻结结论

- [x] 已明确 `phase02-12` 的直接上游是 `phase02-09`、`phase02-10`、`phase02-11` 与 `phase02-11A`
- [x] 已明确联调必须在真实后端模式下执行，而不是 mock 模式
- [x] 已明确数据库迁移、种子数据与后端运行入口构成当前验收环境前提
- [x] 已明确空状态到 `CreateModule` 的最小闭环必须被验证
- [x] 已明确 `CreateRelease` 成功回流与版本列表更新必须被验证
- [x] 已明确 `BindModuleToProduct` 与 `MapModuleToRepository` 成功后停留详情页并刷新结果必须被验证
- [x] 已明确空状态、错误态与返回路径必须进入本次验收范围
- [x] 已明确返回列表时恢复 `queryText` 与 `statusFilter` 必须被验证
- [x] 已明确无效 `moduleId`、非法状态值与唯一约束冲突等关键异常路径必须被验证
- [x] 已明确 `Decision` 只作为详情读取中的附属入口，不扩写为新的写入主线
- [x] 已明确 `.proto` 是当前阶段唯一合同源，联调时必须以其为核对基准
- [x] 已明确验收结果必须形成可复核证据，并将发现的问题收口到当前阶段

# Personal Software Company OS

# MVP0.5 Final Consensus & Planning Baseline

**Author:** GPT54  
**Date:** 2026-08-14  
**Purpose:** 基于五份 `mvp0.45` 专家评审文档与当前仓库现实，形成 PSCO 在 `phase10` 收口之后、进入下一阶段正式 `/plan` 之前的最终仲裁结论、范围基线、推进重点、非目标与验收方向。

---

## 1. 文档定位

本文不是第六份 `mvp0.45` 评审意见，也不是直接替代后续正式 `/plan` 的执行文档。

本文要解决的是五件事：

1. 基于五份 `mvp0.45` 评审，明确哪些判断已经形成稳定共识；
2. 对仍有分歧但已经足够仲裁的问题给出最终取舍；
3. 冻结 `mvp0.5` 的正式方向、范围与非目标；
4. 约束下一阶段正式 `/plan` 的输入边界与推进顺序；
5. 为后续 `/plan -> /spec -> 实现 -> 验收 -> 收口` 提供唯一上游判断基线。

本文的职责，是为下一阶段正式 phase 入口提供上游依据，而不是在这里越权冻结正式 `phase` 名称、`.trae/specs/phaseXX_*` 路径、接口名、字段名或实现细节。

## 1.1 本文与 `PSCO-mvp04-summarize-feedback.md` 的关系

本文与 `PSCO-mvp04-summarize-feedback.md` 的关系，明确冻结为：

1. `PSCO-mvp04-summarize-feedback.md` 已经完成了 `mvp0.4` 的总方向仲裁，并冻结了：
   - `Asset-Action Closure` 为唯一中心主线；
   - `Agent Consumption Layer` 为受控支撑能力；
   - `Cross-Project Convention Asset` 为候选探索。
2. `phase10` 已经完成了围绕 `Asset-Action Closure` 的主要正式交付与收口；
3. 围绕 `mvp0.45` 的五份专家评审，已经进一步把“下一阶段到底该推进什么”从开放讨论压缩到少量可仲裁问题；
4. 本文要解决的，不是再次判断 `phase10` 做得对不对，而是正式收敛：在 `Asset-Action Closure` 收口后，PSCO 进入 `mvp0.5` 时应以什么方向继续推进；
5. 因此，本文既承接 `mvp0.4` 的正式共识，也新增冻结 `mvp0.5` 必须优先完成的内容：根级上下文真相源治理与最小只读项目上下文导出。

换句话说：

> **`mvp0.4` 解决的是“PSCO 能否把经营骨架与动作链真正闭合”；`mvp0.5` 要解决的是“PSCO 能否以 Go 后端为中心，稳定地把现有项目上下文提供给 agent 消费，并先治好自己上下文源的漂移问题”。**

---

## 2. 证据来源

## 2.1 本轮五份直接评审输入

- `docs/review/PSCO-mvp045-GPT54.md`
- `docs/review/PSCO-mvp045-DPv4pro.md`
- `docs/review/PSCO-mvp045-gemini31pro.md`
- `docs/review/PSCO-mvp045-GLM52.md`
- `docs/review/PSCO-mvp045-qwen38max.md`

## 2.2 当前最终共识与规则基线

- `PSCO-mvp04-summarize-feedback.md`
- `AGENTS.md`
- `plan.md`
- `project_rules.md`
- `TECH_STACK_BASELINE.md`
- `architecture_map.md`

## 2.3 当前正式仓库现实

- `phase10` 已完成正式收口；
- 后端采用 `Go + ConnectRPC + PostgreSQL` 单一正式主线；
- 前端采用 `React + TanStack Router + TanStack Query` 单一正式主线；
- 当前仓库已经具备稳定运行的四大核心实体与 detail/review/dashboard/onboarding 主线；
- 当前仓库同时已经暴露出根级静态文档重复承载、引用漂移与 agent 上下文重建成本过高的问题。

## 2.4 本文采用的仲裁原则

1. **工程现实优先于理论推演。** 以当前仓库已存在的结构、合同、实现与使用摩擦为准，而不是抽象想象。
2. **高共识优先于继续争论。** 对已经在五份文档中形成稳定共识的判断，不再反复开放讨论。
3. **只读消费优先于自动维护。** 当前阶段先证明 agent 能稳定消费上下文，再谈受控维护。
4. **治理既有真相源优先于新通道扩张。** 若现有根级上下文本身存在漂移，再建新通道只会放大错误。
5. **不越权冻结实现细节。** 正式 phase 命名、spec 路径、接口名与字段名留给后续 `/plan` 与 `/spec` 决定。

---

## 3. 最终共识

经过五份 `mvp0.45` 评审交叉比对，以下内容已经形成稳定共识，可直接视为 `mvp0.5` 的正式前提。

## 3.1 关于 PSCO 当前定位的共识

1. 当前 PSCO 已经不是粗糙 CRUD 原型，而是具备经营语义、资产关系与动作承接能力的结构化系统；
2. 当前 PSCO 的主要问题，不再是“缺少更多对象”，而是“如何把既有上下文稳定提供给 agent 消费，并降低真实协作中的上下文恢复成本”；
3. 当前 PSCO 更适合被定义为：
   - 项目上下文系统；
   - 规则与约束系统；
   - 资产关系系统；
   - agent 可消费的后端能力面；
4. 当前 PSCO 不应被定义为开发流程控制器，不应规定 IDE 现场 agent 的微观开发顺序。

## 3.2 关于 web 与 agent 渠道关系的共识

1. web 不退化，继续作为 PSCO 的正式交互渠道之一；
2. agent 渠道可以正式进入，但进入方式必须克制；
3. web 与 agent 不应作为对称双主线并行建设，而应采取“轻重并行”；
4. 二者必须共享同一套 Go backend canonical core；
5. 不允许 web 和 agent 各自长出第二套语义、第二套流程或第二套事实源。

## 3.3 关于 agent 路线的共识

1. agent 渠道当前应先做消费，不做维护主线；
2. 当前阶段只允许只读消费，不允许 agent 自动写回；
3. 不应在当前阶段引入 Draft、审批流、agent 专属写入路径或 agent 专属一级业务对象；
4. 只读消费的第一目标，不是建新的重型协议层，而是先验证 agent 是否能稳定拿到正确上下文。

## 3.4 关于四实体关系的共识

1. `Product / Repository / Module / Decision` 的现有结构不需要推倒重来；
2. 下一阶段需要的是语义澄清，而不是结构重构；
3. `Module` 更适合作为可复用能力资产理解，允许后置提炼，不要求一律前置规划完成；
4. `Decision` 更适合作为规则、约束、选择与依据的索引对象理解，而不只是文本卡片；
5. 以上判断应作为 baseline 语义确认，而不应直接触发一轮实体重设计。

## 3.5 关于下一阶段实现切入点的共识

1. 下一阶段不宜先做 MCP、CLI、前端对话框、自动扫描、知识图谱或重型 GitHub 集成；
2. 下一阶段最合理的实现型主交付，是最小只读“项目上下文聚合导出”能力；
3. 下一阶段最合理的治理型前置工作，是根级静态上下文文档的单一真相源治理；
4. 下一阶段的第一个正式消费场景，应优先以 PSCO 仓库自身作为 dogfooding 对象，验证上下文恢复成本是否真实下降。

---

## 4. 最终仲裁结论

## 4.1 `mvp0.5` 的版本语义

**最终结论：**

> `mvp0.5` 的实质任务，不是开启新一轮概念扩张，也不是直接把 PSCO 做成完整 agent 平台；  
> 而是在 `phase10` 完成 `Asset-Action Closure` 后，把 PSCO 正式推进为一个可被 agent 稳定消费的项目上下文系统，并先治好它自身上下文入口的重复、漂移与悬空引用问题。

这意味着我正式采纳以下判断：

1. `mvp0.5` 的中心问题不是继续扩实体，而是降低上下文恢复成本；
2. `mvp0.5` 的主交付不是 agent 自动维护，而是只读消费；
3. `mvp0.5` 不以新协议层为起点，而以现有 backend canonical core 上的最小聚合导出为起点；
4. `mvp0.5` 必须同步处理根级静态文档双源与漂移问题，否则新的 agent 消费入口会建立在不稳定的上下文源之上。

进一步冻结为：

> **`mvp0.5` 不是让 PSCO“控制” agent，而是让 PSCO 先成为 agent 能正确读取、正确理解、低成本接手的上下文系统。**

## 4.2 `mvp0.5` 的总主题

**最终结论：**

> `mvp0.5` 的总主题确定为：  
> **从已闭合的经营动作系统，走向可被 agent 稳定读取的项目上下文系统。**

它的正式主轴收敛为两件事：

1. **根级上下文真相源治理**（前置必做）
2. **最小只读项目上下文导出**（中心主交付）

## 4.3 关于阶段组织方式的仲裁

对于“是否先拆一个纯方向收敛 phase，再拆一个交付 phase”的分歧，本文正式仲裁为：

> **不建议再拆一个纯讨论 phase；建议在一个交付型 phase 内，同时承接边界收敛、根级治理、最小导出实现与验收。**

原因如下：

1. 当前原则层共识已经足够稳定，再追加纯讨论 phase 的边际收益开始变低；
2. 当前需要确认的“方向与边界”工作量更适合作为交付 phase 内的边界收敛类子任务；
3. 单人长期维护项目不适合为纯文档收敛单独支付完整 phase 流程成本；
4. 当前最重要的是尽快把已收敛的方向转化为一轮可验证交付。

## 4.4 关于静态文档治理方式的仲裁

对于“是否做静态文件全量 backend 派生”的分歧，本文正式仲裁为：

> **当前阶段不做全量派生；当前阶段只做单一写者规则执行、重复承载收敛与悬空引用修复。**

正式取舍如下：

1. `plan.md` 继续作为阶段状态与推进路线的唯一正式承接位；
2. `architecture_map.md` 继续作为目录结构与文档落点的唯一正式承接位；
3. `TECH_STACK_BASELINE.md` 继续作为技术栈正文的唯一正式承接位；
4. 其他根级入口文档应改为摘要式引用，不再重复承载同一主结论；
5. 对不存在文件的入口引用必须修正；
6. “静态文件从 backend 全量派生”保留为远期候选，不进入当前阶段。

## 4.5 关于最小交付物的仲裁

本文正式采纳五份评审中的高共识判断，并冻结：

> **下一阶段最小可验证交付物 = 项目上下文聚合只读读取能力 + AGENTS 风格上下文导出能力。**

其边界进一步冻结为：

1. 该能力以现有 Go backend canonical core 为基础；
2. 该能力是只读的；
3. 该能力是聚合投影，不是新一轮实体重构；
4. 该能力应首先服务 PSCO 仓库自身的 agent 协作场景；
5. 当前阶段不要求同时落地 MCP、CLI 与多协议出口，优先验证“上下文是否真的可被稳定消费”。

---

## 5. `mvp0.5` 正式范围基线

## 5.1 前置必做范围

在正式进入 `mvp0.5` 主交付实现之前，必须先完成以下工作：

1. 明确写入下一阶段 baseline：
   - PSCO 是上下文系统，不是开发流程控制器；
   - agent 先消费、后维护；
   - web 不退化，agent 受控最小进入；
   - 四实体做语义澄清，不做结构重构。
2. 完成根级静态文档单一真相源治理：
   - 消除重复承载；
   - 修复悬空引用；
   - 明确唯一正式承接位。

## 5.2 必做范围

下一阶段正式 `/plan` 必须覆盖以下两组正式范围：

1. **根级上下文真相源治理**
   - phase 状态、目录落点、技术栈正文的唯一承接位收敛；
   - 根级文档之间重复结论去重；
   - 不存在文件的引用修复或替换；
   - 让 agent 当前直接消费的根级文档恢复单值一致。

2. **最小只读项目上下文导出**
   - 基于现有 canonical 数据提供按项目/仓库聚合的只读上下文读取能力；
   - 输出可供 agent 直接消费的结构化上下文；
   - 提供 AGENTS 风格或等价 Markdown 风格导出；
   - 用 PSCO 仓库自身完成 dogfooding 验证。

## 5.3 候选范围

以下内容可作为 `mvp0.5` 候选范围，但不构成主线阻断项：

1. 上下文导出的多格式输出增强；
2. 在不引入第二事实源的前提下，对现有 Export 能力做轻量复用；
3. 对静态规则入口做轻量指针文件优化。

## 5.4 明确不做范围

当前阶段明确不做：

1. MCP 协议层正式实现；
2. CLI 工具正式实现；
3. agent 自动写入、Draft 接口、审批流；
4. web 前端对话式 agent 入口；
5. 知识图谱、自动扫描、重型 GitHub/Gitea 集成；
6. 四实体结构重构或大规模 schema 扩张；
7. 主动向外部仓库注入文件；
8. 静态文件从 backend 全量派生。

---

## 6. 对后续 `/plan` 的正式输入

## 6.1 推荐的主交付定义

我建议后续正式 `/plan` 至少围绕以下单一主交付能力展开：

> **让 agent 通过单一稳定入口，读取当前项目的完整核心上下文。**

该主交付下的子任务，应至少覆盖：

1. 边界收敛类：
   - 四实体语义确认；
   - 上下文系统定位；
   - web / agent 分工说明；
   - 不做清单冻结。
2. 实现设计类：
   - 项目上下文聚合只读读取设计；
   - AGENTS 风格导出设计；
   - 与现有 canonical 数据对齐设计。
3. 源代码实现类：
   - 最小只读聚合读取能力；
   - 最小文档导出能力；
   - 必要的根级入口同步。
4. 验证验收类：
   - 使用 PSCO 仓库自身进行 dogfooding；
   - 验证新接手模型的上下文恢复成本是否下降；
   - 完成对应的工具链与文档一致性验证。
5. 根级同步类：
   - 回写根级入口；
   - 同步最新阶段状态；
   - 留档下一阶段进入条件。

## 6.2 对正式 `/spec` 的硬约束

1. `.proto` 仍是唯一长期合同源；
2. 新增上下文导出能力必须保持只读；
3. 不得引入 agent 专属一级业务对象；
4. 不得长出第二套 canonical API；
5. 不得把 PSCO 做成 IDE 现场的流程控制器；
6. 不得通过实现细节反向改写四实体正式语义；
7. 不得让根级文档继续重复承载同一主结论。

## 6.3 对后续验收的硬要求

1. 新接手 agent 是否能通过少量固定入口恢复当前项目核心上下文，而不再需要阅读大量根级文档与历史评审；
2. 根级文档是否消除了重复状态清单、重复落点清单与悬空引用；
3. 新增导出能力是否完全只读；
4. 新增能力是否服务于 PSCO 仓库自身的真实协作场景，而不是停留在抽象设计；
5. 当前阶段是否严格没有引入 agent 写入路径、第二套语义或第二套事实源。

---

## 7. 最终结论

**一句话总结：**

> `mvp0.5` 不是继续扩功能，也不是先做 agent 自动化；而是先把 PSCO 已有的上下文系统做对、做稳、做成 agent 真能读懂和接手的形式。

最终冻结为：

1. `mvp0.5` 的唯一中心交付，是 **最小只读项目上下文系统**；
2. 其前置条件，是 **根级上下文真相源治理**；
3. PSCO 的正式定位是 **上下文系统**，不是 **开发流程控制器**；
4. agent 路线当前只做 **消费**，不做 **维护**；
5. web 不退化，但 agent 只以受控最小能力面并行进入；
6. 正式 `/plan` 应围绕上述结论展开，而不是重新打开已经收敛的问题。

我对本轮五份专家文档的最终仲裁是：

> **方向已经足够明确，不再需要继续扩大原则讨论轮次。**
> **下一步应进入正式 `/plan`，以“根级真相源治理 + 最小只读上下文导出”作为 `mvp0.5` 的单一主交付方向。**

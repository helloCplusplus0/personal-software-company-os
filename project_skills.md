# project_skills.md

# PSCO Project Skills

## 1. 项目核心语义

PSCO 的长期语义链条仍然成立：

`Opportunity -> Venture -> Product -> Feature -> Module -> Release -> Capability -> Decision`

但当前执行阶段只收敛其中一部分进入 `v0.1`。

## 2. 当前执行语义

`v0.1` 当前优先承载的最小闭环是：

`Product -> Module -> Release -> Decision -> Repository Binding -> Feedback`

## 3. 当前最重要的设计原则

- 不让用户感觉自己在“填系统”
- 不把未来愿景对象做成当前重实体
- 不把自动化当成当前价值来源
- 不把目录整理误当成设计推进

## 4. 当前最重要的风险提醒

- 过度概念化
- 过度协议化
- 过度自动化
- 过度前置 GitHub 集成
- 把 PSCO 做成“模块清单工具”而失去 `Decision` 差异化

## 5. 进入 MVP Spec 前的固定检查项

1. `Decision` 是否还在 MVP 中？
2. `Capability` 是否仍是派生层？
3. `Rust` 是否仍然被排除在 `v0.1` 外？
4. `Venture` 是否仍然是可选实体？
5. 是否已经明确冷启动、导入路径与导出要求？
